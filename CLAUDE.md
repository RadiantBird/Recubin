# CLAUDE.md — C++ Game Engine Project

このファイルはすべての会話の共通前提。必ず従うこと。

---

## プロジェクト概要

- **言語**: C++23
- **グラフィックス**: OpenGL
- **ビルドシステム**: カスタム Python ベース
- **目標**: 独自ゲームエンジン
- **環境**: Windows 11 Home

---
## セッション開始時（必須）

1. progress.md の最新セッションを読む
2. 未解決・保留事項があれば確認する
3. その上で今回の指示を待つ

## 計画について

- 計画・設計はこのセッションのモデル自身が行うこと
- 探索エージェントの呼び出しは許可します
- Taskツールやサブエージェントに計画を外注しない
- 理由: 計画にはコンテキストが必要。外注すると再クローリングが発生し、
  時間もかかり、計画の質も落ちる
---

## 絶対ルール（最優先）

### スコープ厳守
- **指示されたファイル・関数だけ**触る
- 指示にないファイルは「読み取り専用」として扱う
- 「ついでに改善」「ついでに整理」は**禁止**
- 最適化・リファクタは明示的に指示された場合のみ行う

### 実装前に確認する
- 変更するファイルと箇所を先にリストアップしてから実装する
- 迷ったら実装せず質問する
- [仕様書](spec.md)に書いてある仕様を確認しておく

### 実装後
- 説明は最小限でよい

---

## コーディング規約

```cpp
// メンバ変数
m_variableName

// クラス名
PascalCase

// 関数名
camelCase

// 定数
UPPER_SNAKE_CASE
```

- ヘッダに実装を書かない（テンプレートを除く）
- 例外は使わない（エラーはリターンコードかオプション型）
- `std::vector` を基本コンテナとして使う

---
## 設計タスクの進め方

設計を依頼する場合、以下の手順で必ず行うこと：

### 1. 影響範囲の洗い出し（実装前に必須）
- 変更・追加対象の機能が既存のどのファイル・クラスに影響するか列挙する
- 暗黙的に必要になる作業（シリアライズ、UI反映、初期化順など）も含める

### 2. 実装順序の決定
- 依存関係を考慮した順番で並べる
- 後から変えにくい部分（インターフェース、データ構造）を先にする

### 3. 不明点の明示
- 仕様が曖昧な箇所は実装せず、質問としてリストアップする
- 「たぶんこうだろう」で進めない

### 設計依頼の形式
```
@対象システム [機能名]の設計だけして
- 目的: [何のための機能か]
- 制約: [既知の制約があれば]
- 暗黙タスクも含めて洗い出すこと
```
---
## ポインタ・メモリ管理
- 原則: `std::unique_ptr` を使用。所有権の移動は `std::move`。
- 共有: `std::shared_ptr` は、PhysXのオブジェクトなど共有が必須な場合のみ。
- 借用: `T&` (参照) を最優先。`nullptr` を許容する場合のみ生ポインタ `T*` を使用。
- 生ポインタの用途:
  - 所有権に関与しない一時的なアクセス
  - C API (OpenGL, PhysX等) への引数
  - nullptr 許容のオプション参照

---
## ビルド
実装し終わったら確認のため、ビルドしてください。
- エディターとランタイム(Recubin.exe/RecubinEngine.exe)
  `python build.py build`でできます。
- ランチャー
  `python build.py launcher`でできます。
---

## アーキテクチャ方針

- 古典的OOP（継承・ポリモーフィズム）を基本とする
- 過度な抽象化より実用性を優先する
- PhysX を物理エンジンとして使用

---

## やってはいけないこと

- [ ] 指示されていないファイルを変更する
- [ ] 関数シグネチャを勝手に変える
- [ ] 「より良い設計」に勝手に置き換える
- [ ] ビルドシステムを触る（明示的な指示なし）
- [ ] コメントを勝手に削除・変更する