NetworkTest

Windows:
  RecubinEngine.exe

または launcher.exe から起動するとデスクトップショートカットを作成できます。

macOS (Apple Silicon):
  ターミナルでこのディレクトリへ移動し、次のコマンドで起動します。

  初回のみ、実行権限が必要な場合:
    chmod +x RecubinEngine-macos-arm64

  起動:
    ./RecubinEngine-macos-arm64
