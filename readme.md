# Recubin -Powering imagination again-

## 概要
Roblox風のローカルソフトウェア開発をするためのエンジン。

## 目標
- Robloxの感覚でソフトウェア（ゲーム）を開発し、ローカルで公開できるようにしたい。
- Robloxでできなかったことを実現する(複数Workspaceとか特殊効果とか)。
- わかりやすいエディターを作る。直感的であれ。
- 物理で遊べるようにする

## 現時点の懸念
- なし

## Todoリスト
(!: 中止|?: 不明|x: 達成済み)
- [x] コードをきれいにして、保守しやすくする
- [x] バグ修正
- [!]**Userが外部に依存しないようにリファクタ（Physicsだけ、windowは入力に必要なので却下）** 
  - [] イベントは消費するようにpublic関数で触れるように
  - [] イベントフラグはprivateに
  - 価値を感じないので今は保留で(スタイルもわからん)

- [x] 特殊効果を追加
  - [x]PostEffectインスタンスを定義
  - プロパティにCRT/Posterization/Pixelizeなどを定義
  - それぞれのシェーダーを混ぜ合わせる
  - 順番はZIndexなどで決定
  
- [x] Tool(ツール)インスタンスを追加
- [x] Toolは両手で持てるようにカスタマイズ(つまり、左手だけ/右手だけ)
- [x] ビューポート画面下部にツール用のホットバーを表示
  - キー番号とツール名を四角いボックスの中に表示
  - 中央揃え
  - 何も持っていない時は非表示
- [x] キー操作をステートによって切り替え(エディター、プレイ中、Proximity Promptを押そうとしている等)

- [x] Terrainインスタンスを追加
  - [x] 角柱(直角三角形の底面を持つものを追加)と立方体で構成される
  - [x] ノイズを用いて自動生成
  - [x] 4x4x4 studsのサイズを想定
  - [x] 原点はブロック座標が整数になるように調整して決定(あとでどこが適切か考えておく)
  - [x] 物理と描画は、いちばん処理が軽くなるであろう方法で実行(いちいちすべてをCubeインスタンスにしない)
  - [x] 部分的な置き換えや削除をできるようにする（C++/Luau APIの用意）
  - [x] ブラシ機能

- [] パッケージャーをテスト
- [] 必要なら修正

そういえばWeldしたアセンブリ剛体をキャラクターにくっつけて帽子みたいなのを作ろうとしたら動かなくて失敗したな。
これも修正する必要がある。
あと、TextLabelとか、Buttonが動作してるのかをスクリプト書いてテストしないといけないね。
あとCtrlLock(プレイ中Fキーで左右切り替え)とFPS視点の実装も考えておこう
- [] 一人称視点を追加
  - カメラの距離がある程度小さくなると、体が非表示になり、頭があった位置にカメラが設定される
  - カメラをピンチアウトすると三人称視点に戻る

- [] CtrlLock(Roblox::ShiftLock)を追加
  - 左コントロールで左側にカメラを設定
  - 右コントロールで右側
  とは思ったが、キーボード的に不便だし、右コントロールに手が届くわけがない。
  左Altも検討したが、親指の負担が大きそうである。
  かといってZ,X,Cとかを使うとユーザーのキーバインドが制限されすぎる。 

- [] Mac対応を開始する
  **Metalは今のところ使用しない**(難易度が高い)
  - レンダラーを抽象化する(OS依存をなくし、関数の実装部分で分岐を行う)...要するにインターフェイス
  - OpenGL4.1に準拠した実装を行う
  - ファイル操作のインターフェイス化
  
- [] 設計ルールを定める
  - どのインターフェイスを使うか
  - エラーハンドリングをどうするか
  - 例外処理はいつ使うか？
  - テンプレートはいつから使わせるか
  - ソースの整頓

- [] studに計算を合わせる
  - PhysXの調整
  - 間違いが起きないように単位変換関数を定義する(m<->stud)
  - 1stud = 0.05m / 1m = 20stud

## 中断された作業
- なし

## 現在の問題
- コピペ実装が多い（構造化されていない）

## 使用中の技術
- C++
- OpenGL
- Luau
- stb_image
- ImGui & ImGuizmo
- YAML
- miniaudio
- PhysX
- Windows bat
- Python

## 使用予定の技術
- DirectX(Windows最適化)
- Vulkan(Linuxなど)

  ### Luar言語用
  - Rust(Luarコンパイラ)

## ブラックリスト
- Metal(あくまでOpenGLのパイプラインを最適化したうえでMacにも配布)
---