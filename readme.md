# Recubin -Powering imagination again-

## 概要
Roblox風のローカルソフトウェア開発をするためのエンジン。

## 目標
- Robloxの感覚でソフトウェア（ゲーム）を開発し、ローカルで公開できるようにしたい。
- Robloxでできなかったことを実現する(複数Workspaceとか特殊効果とか)。
- わかりやすいエディターを作る。直感的であれ。
- 物理で遊べるようにする

## 現時点の懸念
- なし

## Todoリスト
(!: 中止|?: 不明|x: 達成済み)

- [x] QoL改善&&カメラ操作など
  - ギズモ操作がよく反転になる問題の改善(Y軸の移動、X軸のリサイズなど)
  - 複数コピー/複数ペースト/複数移動(親変更)の追加
  - Altキーでマウスをロックする(ビューポート中央に固定)
  - 整数に丸める(Vector3型入力ボックスの隣にボタンとして配置)
  - GUI系インスタンスで文字を持つものにFontSizeプロパティを追加(既存のものはデフォルトのサイズを維持)
  - お互いにコリジョンがなくても溶接されている場合は位置関係を保って移動するように修正

#4 整数に丸めるボタン（← 今回これを実装）
#5 FontSize プロパティ
#3 Alt でマウスロック（ビューポート中央固定）
#2 複数コピー/ペースト/移動（親変更）
#6 溶接の相対移動（非コリジョン時）
#1 ギズモ反転（Y移動/Xリサイズ）
上から順にやる

- [x] パッケージャーをテスト
  - [] 問題あり: ImGuiコンテキスト未生成でアクセス違反、これの修正から
- [x] 必要なら修正

- [x] TextLabel/Buttonが動作してるのかをスクリプト書いてテスト
  - [x] Hovered Signalを両者に追加する(要素中にマウスカーソルが入ったときに発火)
- [x] Userインスタンス関係の更新
  - [x] GLFWwindowへの依存を無くす(将来的なインターネット通信を考慮したため)
  - [x] User.Inputの定義
    - イベント
      - User.Input.Pressed(キーボード/マウス含め)
      - Released
    - bool関数
      - IsPressed(key)
    押されたキーはstringで取得できるようにする(tostring?)
  - Humanoid更新
    - Healthプロパティを追加
      - デフォルト
        - Health = 100
        - MaxHealth = 100
      - 0以下になった場合はDiedイベントを発火し、
        キャラクターをパージした後、
        ランダムに力を加えて吹き飛ばす。
        そのあとrespawnTime後に再生成される
  - [x] Luau APIに開放

- [x] Vector3型がLuau側で演算できない問題を修正(ほかにも演算可能であるべき型がある可能性があるので、それも修正する)
  例: `local a = Vector3.new(0,1,2) + Vector3.new(1,2,3)`
  -> `attempt to perform arithmetic (add) on userdata`(status 2)

- [x] 3Dモデルローダーを実装
  - GLBファイルを受け付ける
  - それ以外はコンバーター行き
  - MeshCubeがGLBファイルの参照を持つ(browseFile関数)
  - テクスチャが含まれていることも想定する
  - 当たり判定はできるだけ形状に近似させた状態にしたい

- [x] アニメーションを実装
  - アニメーションエディターを新しいウィンドウとして作成
  - 再生/一時停止/終了のボタンを追加
  - アニメーションエディターは再生バーをドラッグして時間tを設定し、
    そのうえでキャラクターを選択してCubeのCFrameの位置を変更する
    アニメーションエディターのフォーカスが切れたら元のCFrameに戻る

  - 時間tにおけるCubeのCFrameをそれぞれ補間関数で滑らかに変更するだけ
  - 補間関数は今のところ、線形/2次/cos/sin/指数などを定義
  - Animationインスタンスに時間とCFrameの情報が書き込まれる
    Humanoidがそれを再生/一時停止/終了する
    Animationには再生速度をセットし、tの増分を変化させる

- [] Mac対応を開始する
  - **Metalは今のところ使用しない**(難易度が高い)
  - レンダラーを抽象化する(OS依存をなくし、関数の実装部分で分岐を行う)...要するにインターフェイス
  - OpenGL4.1に準拠した実装を行う
  - ファイル操作のインターフェイス化

## 中断された作業
- なし

## 現在の問題
- コピペ実装が多い（構造化されていない）

## 使用中の技術
- C++
- OpenGL(GL/GLFW)
- Luau
- stb_image
- cgltf
- ImGui & ImGuizmo
- YAML(yaml-cpp)
- miniaudio
- PhysX
- Windows bat
- Python

## 使用予定の技術
- DirectX(Windows最適化)
- Vulkan(Linuxなど)

  ### Luar言語用
  - Rust(Luarコンパイラ)


## ブラックリスト
- Metal(あくまでOpenGLのパイプラインを最適化したうえでMacにも配布)
---