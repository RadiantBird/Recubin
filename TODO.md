## Todoリスト
(!: 中止|?: 不明|~: 保留中|x: 達成済み)

### 定期
- 長すぎる関数がないか見張る
- 直書きすぎて保守が困難でないか見張る
- コミット前に一応読む

### エディター関係

- [ ] Macパッケージもリリース
  - [ ] .app形式で間違ってパッケージされているせいでバイナリが対応できておらずファイルIOが異常。
    Windowsと同じファイル形式でパッケージするように修正する。

# 物理エンジン関係

- N/A
 
### レンダリング関連

- [~] BaseCubeマテリアルに基づくPBRレンダリング(保留)
  - 反射率プロパティを新しく追加
  - レイトレーシングはしない
  - Q. これはOpenGL-4.1で可能？

### ネットワーク関係

- [x] クライアントはほかのクライアントのキャラクターを見ることができない。回帰バグ、というか物理操作の変更による類似バグだと思う。直す。

- [!] IPv4 NAT越え（STUN＋ランデブー＋UDPホールパンチ）を完了する
    - [x] ルームコード作成/参加、候補交換、admission token検証、ENet接続、同一ソケットでのホスト移行を実装
    - [x] ローカルのC++ NAT codec回帰テストとPythonランデブー統合テストを実装
    - [!] 公開配置したSTUN/ランデブーを使い、異なる2回線で接続を実地検証
      - サーバー代くれくれ

    - [ ] NAT越しの3ピアでホスト移行とPeerId維持を実地検証
      - 3つも端末がないので、Windowsでローカルホスト経由で2ピア、NAT越しのMac1台でテスト
    - TURN/リレーとIPv6は今回の対象外。上記2件の外部検証が済むまで完了扱いにしない
    - [x] Tailscale接続が成功
    - [ ]「direct接続失敗後も Connecting のまま」という診断不備の修正

- [?] 全workspaceを監視し、オブジェクトの生成/削除を同期する

- [~] NetworkEvent

### その他

- N/A

### 最重要

#### クレーム
  ```
  最近のCodexのリファクタ方針について不満があります。問題は、個別ケースを大量にハードコードしてから後で共通化する流れになっていることです。

  たとえばPropertiesPanelでは、Script、Terrain、Sound、PostEffectなど各Instanceごとに手書き処理や個別分岐が残り、複数選択時のプロパティ処理も後付けで対応されています。こちらが未実装を指摘すると、その時点で初めてスキーマ統合やmultiEditKey、共通レンダラなどの設計が導入されました。

  しかし本来は、最初から「各クラスがエディタへ公開するプロパティを自己記述する」構造にすべきだと考えます。

  概念的には次のような形です。

  class ABCD {
  public:
      void fxxk();

      static constexpr auto EditorProperties = {
          Enabled,
          Material,
          Source
      };
  };

  エディタ側は各クラス固有の事情を知る必要はなく、

  for (auto& property : instance->EditorProperties()) {
      DrawProperty(instance, property);
  }

  のように、宣言されたプロパティを共通処理で描画・編集するだけにします。

  重要なのは具体的なC++構文ではなく、責務の分離です。

  PropertiesPanelが「ScriptにはEnabledがある」「TerrainにはDataPathがある」「SoundにはVolumeがある」と知る構造ではなく、各Instance自身が「私はこれらのプロパティをエディタへ公開する」と宣言し、Editorはそれを機械的に処理する構造にしてください。

  この方式なら、新しいInstanceを追加した際にもPropertiesPanelへ個別分岐を追加する必要がなくなります。複数選択も、各InstanceのPropertyDescを集約し、型・multiEditKey・EditorWidgetなどの互換性だけ確認すれば一般化できます。

  今回特に問題だと感じたのは、「全インスタンスへ一括移行」という要求に対し、途中の基盤実装だけで作業を完了扱いした点です。後から未実装を指摘すると、実装不能なブロッカーではなく、単に途中で区切っただけだと説明されました。

  今後は、個別ケースを列挙して大量にハードコードする前に、必ず以下を検討してください。

  同じ構造を持つ処理をデータ駆動・スキーマ駆動にできないか
  新しい型を追加したとき、中央のEditorコードを変更せずに済むか
  同種の変更を複数クラスへ手作業で繰り返していないか
  要求された完了条件を、途中の「基盤ができた」「ビルドが通った」に勝手に縮小していないか
  既存のハードコードを増やすのではなく、ハードコードそのものを減らせないか

  AIを使う理由の一つは、単純作業を高速に大量生成することではなく、人間なら「これを毎回書くのはおかしい」と気づく反復を構造化することです。

  20クラスに同じ変更を手作業で入れられることは利点ではありません。20クラスを個別に変更しなくて済む設計を先に考えてください。

  特にリファクタリングでは、「今見えているバグを最短で直す」ことよりも、「同じ種類のバグが次のInstance追加時に再発しない構造にする」ことを優先してほしいです。
  ```

- [ ] 全InstanceのPropertiesをスキーマ駆動へ移行する
      - Toolなど、YAMLとLuauで値の表現が異なる特殊プロパティを共通スキーマ化する
      - FilePath／InstanceReferenceの複数選択編集を共通レンダラへ統合する
      - Terrain生成などの特殊操作をEditorActionDescへ移行する
      - PropertiesPanelに残るInstance別の手書き分岐を整理し、新規Instance追加時に中央Editorを変更不要にする

- [ ] リカバリーはきちんと機能しているが、ようこそタブが邪魔で消えたように見えるし、
      間違って押すと危険なので異常を検知したときはようこそタブを有効化できないようにし、
      非表示の状態でウィンドウを出す。
      
------------------------------------------------------------------------
## Todo++リスト -v2.0-

- p2pネットワーク
- Programインスタンス(Recubinヘッダーで動かすexeをIPC)
- midiプレーヤー
- FrutigerAeroなエディター
  - まずアンチエイリアシング
- パラレルLuau
- Luar言語にまともに取り組む
  - サブモジュールにする
- ネットワークによるエディターの共同作業の実現 ← gitでいいと思う
- LuauエディターMODの実装
  - ImGuiウィンドウを作成できる
  - 既存スクリプトとは明確に区別
  - テストスクリプト例: Cubeを円状に配置する

------------------------------------------------------------------------
