(おせっかいなボタンを消すためにわざと"T0do"と書いてあります)
## T0doリスト
(!: 中止|?: 不明|~: 保留中|x: 達成済み)

### 定期
- 長すぎる関数がないか見張る
- 直書きすぎて保守が困難でないか見張る
- コミット前に一応読む

### エディター関係

- [x] スキーマ移行の影響でSpatialのVector3が入力しずらくなった。
      以前のようにひとつのテキストボックスを追加し、
      `1, 2, 3`というように入力できるようにする。
      CFrameも同様に、
      `4, 5, 6, 30, 60, 90`
      というように、最初の3つを位置、後半を度数法の角度
      のテキストボックスを用意する。
      Color4も今回の改善で4つのボックスと1つの一発設定を用意するが、
      従来のImGuiカラーパレットも開けるようにボタンを用意する。
      
- [?] Macパッケージもリリース
  - [?] .app形式で間違ってパッケージされているせいでバイナリが対応できておらずファイルIOが異常。
    Windowsと同じファイル形式でパッケージするように修正する。
    - autosaveディレクトリも考慮する

- [x] エクスプローラー（ヒエラルキー）の要素の並べ順を定義する
  - 違うインスタンスの場合
    - 重要なインスタンスを最初に並べる
      (Workspace > Folder > Model > Script > LocalScript > ModuleScript > ファイル類 > ValueBase > BaseCube > ...)
    - 優先順位が定義されていないインスタンスは最後尾に並べる
    - インスタンスは同じ種類ごとにまとめて並べる
  - 同じインスタンスの場合
    - 名前をASCII順に並べる
    - 名前に数字が含まれる場合、その数字は数値として比較し、小さい順に並べる
      例:
      Cube1
      Cube2
      Cube3
      Cube10
      Cube20
  - 名前を変更した場合は、編集確定後に並べ直す
  - 並べ替えによって選択状態や展開状態が失われないようにする

  例:
  Cube1
  Cube3
  Cube2
  Script
  IntValue
  ↓↓↓
  Script
  IntValue
  Cube1
  Cube2
  Cube3

- [x] Toolがアップデートで壊れてないかテストする

- [x] キャラクター移行スクリプトをtriangle.rcbnの設定を参考に修正しておく。
      - あと、C++側のリグ生成も間違いになってる感じなので、それも上記通りに修正。

# 物理エンジン関係

- [x] キャラクター修正でbaseplate.yamlの車が爆発してもう終わってる
      なぜ？？？

```
- [x] キャラクターに溶接したAnchoredでないCubeがあるとなぜか荒ぶる問題を修正
  - 特に腕に溶接したものがあらぶりやすい。計算ミス？

- [x] キャラクターの最初の向きを変えると高速でフリップして操作できない。この問題を修正する
- [x] キャラクター(Anchored済みで固定されている状態のモデル)に溶接したCubeが、実行時に膨張する(Sizeの問題ではなく、CFrameの内部バグである可能性が高い)問題の修正
```
↓↓↓
物理キャラクターリグにして解決した。
その結果いろいろインスタンスが増え、座標系が堅牢になり、エンジンの安定性が向上した。
素晴らしい仕事。


### レンダリング関連

- [x] Weatherクラスで雲の色を変更可能にする(Color4)
- [~] BaseCubeマテリアルに基づくPBRレンダリング(保留)
  - 反射率プロパティを新しく追加
  - レイトレーシングはしない
  - Q. これはOpenGL-4.1で可能？

### ネットワーク関係

- [x] クライアントはほかのクライアントのキャラクターを見ることができない。回帰バグ、というか物理操作の変更による類似バグだと思う。直す。

- [!] IPv4 NAT越え（STUN＋ランデブー＋UDPホールパンチ）を完了する
    - [x] ルームコード作成/参加、候補交換、admission token検証、ENet接続、同一ソケットでのホスト移行を実装
    - [x] ローカルのC++ NAT codec回帰テストとPythonランデブー統合テストを実装
    - [!] 公開配置したSTUN/ランデブーを使い、異なる2回線で接続を実地検証
      - サーバー代くれくれ

    - [ ] NAT越しの3ピアでホスト移行とPeerId維持を実地検証
      - 3つも端末がないので、Windowsでローカルホスト経由で2ピア、NAT越しのMac1台でテスト
    - TURN/リレーとIPv6は今回の対象外。上記2件の外部検証が済むまで完了扱いにしない
    - [x] Tailscale接続が成功
    - [ ]「direct接続失敗後も Connecting のまま」という診断不備の修正

- [?] 全workspaceを監視し、オブジェクトの生成/削除を同期する

- [~] NetworkEvent

### その他

- [x] たまに並列リンカーがファイルロックで失敗しちゃうのでこれを直す

- [ ] luarの定義ファイルでクラス書いてそれでなんか書けたらいいのに

### 最重要

- N/A
      
------------------------------------------------------------------------
## T0do++リスト -v2.0-

- p2pネットワーク
- Programインスタンス(Recubinヘッダーで動かすexeをIPC)
- midiプレーヤー
- FrutigerAeroなエディター
  - まずアンチエイリアシング
- パラレルLuau
- Luar言語にまともに取り組む
  - サブモジュールにする
- ネットワークによるエディターの共同作業の実現 ← gitでいいと思う
- LuauエディターMODの実装
  - ImGuiウィンドウを作成できる
  - 既存スクリプトとは明確に区別
  - テストスクリプト例: Cubeを円状に配置する

------------------------------------------------------------------------
