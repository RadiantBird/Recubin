# AGENTS.md — Recubin Codex 作業規約

## プロジェクト前提

- C++23 と OpenGL で実装する独自ゲームエンジン。
- 主環境は Windows 11。Codex は WSL2 から Windows 側のビルドを実行する。
- 現行の物理バックエンドは **Box3D**。
- **PhysX は廃止済みのレガシーコード。**
- 明示的に依頼されない限り、PhysX の調査・変更・互換対応・新規実装・検証を行わない。
- レガシー実装との互換性を理由に、現在の設計を歪めない。

---

## セッション開始時

1. `progress.md` の末尾にある最新項目と未解決事項を読む。
2. 変更対象に関係する `spec.md` と `doc/` 配下の文書を読む。
3. この `AGENTS.md` の設計ルールを確認する。
4. 実装前に、変更予定のファイル・クラス・関数と変更理由を列挙する。
5. 仕様が曖昧な場合は推測で実装せず、編集前に質問する。

過去の会話要約より、リポジトリ内の仕様・設計文書・現在のソースコードを優先する。

---

## 作業範囲と役割

- 必要な実装であれば、多数のファイルを変更してよい。
- ファイル数を減らすことを目的に、不自然な実装、重複、暫定処理、互換ハックを入れない。
- 指示された機能と無関係な便乗最適化・整理・リファクタは行わない。
- ただし、対象実装そのものに重複や設計違反がある場合は、正しい実装に必要な範囲で整理・共通化してよい。
- 既存コメント、ユーザーの未コミット変更を保護する。
- 関数シグネチャや既存構造の変更が必要な場合は、理由を明確にする。
- 壊れた設計やポリシー違反を「既存コードだから」という理由だけで温存しない。
- 計画・設計と影響範囲の判断はメインエージェントが行う。
- 設計確定後の大きな実装・編集は `implementer` に具体的な対象と変更内容を渡して委譲し、メインエージェントが成果をレビューする。
- メインエージェント自身の編集は、成果物の微修正または軽微な変更を基本とする。
- サブエージェントの出力を無条件に採用しない。必ずソースをレビューする。
- `CLAUDE.md` と `.claude/` は Claude 用設定としてそのまま保持し、Codex の必須参照にせず、変更・削除しない。

---

# 設計ルール

## コピペより共通化を優先する

似た処理を新しく書く前に、既存の関数・テンプレート・ユーティリティ・マクロを探す。

同じ形式の処理が複数箇所に現れる場合は、

- 関数
- テンプレート
- クラス
- 共通インターフェイス
- マクロ

などで統一できないか検討する。

似た `if` 文やチェック処理を複数箇所へコピーして放置しない。

ただし、無理な抽象化によってコードが読みにくくなる場合は共通化しない。

---

## マクロを悪とみなさない

マクロは手段の一つとして使用してよい。

- 型安全に実現できる場合はテンプレートや関数を優先する。
- 式名の文字列化、ソース位置、反復チェックなど、プリプロセッサでしか自然に実現できない用途ではマクロを使用してよい。
- マクロを避けるためだけに重複コードを量産しない。

---

## エラーを隠さない

**不正状態を無言で握り潰してはならない。**

以下のようなコードを書く前に、その条件が正常系か異常系か判断する。

```cpp
if (!value) return;
```

正常な no-op であれば無言でよい。

例:

- 既に登録済み
- `Enabled == false`
- 明示的に処理対象外
- オプション値が存在しないこと自体が仕様

一方、本来存在するべきものが存在しない場合は必ず報告する。

例:

- 必須ポインタが null
- Instance reference の解決失敗
- Box3D body が存在しない
- Box3D joint の生成失敗
- native handle が不正
- 必須リソースのロード失敗
- 不正な数値
- 到達すべきでない状態

可能ならログには次の情報を含める。

- ClassName
- Name
- full path
- 関連する Instance 名
- 問題になった値
- ファイルと行番号

null チェックとは、単にクラッシュを避けて return することではない。

**異常を検出し、観測可能にすることまで含めてチェックとする。**

AI自身が後から推測しなければ原因を特定できないような silent failure を作らない。

---

## チェック処理を統一する

同じ null / validity チェックを繰り返す場合は、既存のチェック関数・テンプレート・マクロを使用するか、共通化を検討する。

例えば、

```cpp
if (!a) return;
if (!b) return;
if (!c) return;
```

を各所へコピーするより、

```cpp
warncheck(a, b, c);
```

のように、失敗した対象を個別に観測できる仕組みを優先する。

複数条件を一つへまとめて、

```cpp
if (!a || !b || !c) return;
```

のように、どの条件が失敗したのか分からなくしてはならない。

---

## 一行へ処理を詰め込まない

行数削減を可読性より優先しない。

特に以下を一行へ圧縮しない。

- 条件分岐
- 複数の副作用
- エラー処理
- 複数の代入
- `return` を伴う処理
- ネストした `if`
- 複数段階の参照解決

悪い例:

```cpp
void setValue(float value) {
    if (std::isfinite(value) && value >= 0.0f) {
        Value = value;
        update();
    }
}
```

読みやすく展開し、失敗理由が重要なら報告する。

縦に長いこと自体は問題ではない。

---

## ソースを読む

「AIが実装したから正しい」と考えない。

実装後は実際のソースを読み、

- どう実装されているか
- 重複していないか
- 不正状態を隠していないか
- 所有権が正しいか
- 既存インターフェイスと統一できないか
- 一時的なハックになっていないか
- 仕様と一致しているか

を確認する。

ビルド成功だけをレビュー完了条件にしない。

---

## ポインタは問題ではない

ポインタそのものを悪とみなさない。

問題なのは、

- 所有権不明
- 借用期間不明
- use-after-free
- dangling pointer
- null の扱い
- 破棄後に無効な参照を残すこと

である。

所有権と寿命に応じて、

- `std::unique_ptr`
- `std::shared_ptr`
- `std::weak_ptr`
- `T&`
- `T*`

を使い分ける。

原則として、

- 単独所有には `std::unique_ptr`
- 本当に共有所有されるオブジェクトには `std::shared_ptr`
- 共有所有物への非所有参照には `std::weak_ptr`
- null を許容しない借用には `T&`
- nullable な非所有参照や C API 連携には `T*`

を使用する。

「スマートポインタだから安全」「生ポインタだから危険」という単純な判断をしない。

破棄時には、関連する登録・handle・参照・constraint なども確実に無効化する。

---

## 例外処理

例外を全面禁止しない。

基本的には明示的な `if`、リターン値、`std::optional` 等を優先する。

ただし、

- パフォーマンスクリティカルではない
- 失敗が通常経路ではない
- 例外によってクラッシュを安全に回避できる
- 呼び出し階層を跨いで失敗を伝える必要がある

場合には例外を使用してよい。

頻繁に例外が発生する設計は異常と判断する。

例外を通常の制御フローとして使用しない。

---

## 最適化の前に実測パフォーマンスを見る

Big-O や理論上の効率だけを理由にコードを複雑化しない。

`O(n^2)` であっても、

- FPS が低下していない
- フレーム時間に問題がない
- 画面が固まらない
- CPU 消費量が異常ではない
- メモリ消費量に問題がない

のであれば、優先して最適化する必要はない。

「アルゴリズムが非効率」というだけのレビューを根拠に変更しない。

まず実際のパフォーマンス問題を確認する。

問題が確認できた場合に計測し、ボトルネックを特定してから最適化する。

---

## ハックより設計を直す

症状だけを隠す処理を追加しない。

例えば、

- null なら黙って return
- 値がおかしければ clamp して隠す
- 参照解決に失敗したら適当な別オブジェクトを使う
- 不整合が起きたら毎フレーム再設定する
- 古い設計を残すためだけの互換分岐を増やす

といった処理は原則禁止する。

原因となる設計・ライフタイム・参照・所有権・データを修正する。

---

## 完成条件

以下だけでは完成とみなさない。

- コンパイルが通った
- クラッシュしなかった
- null チェックを追加した
- テストが一つ通った

少なくとも、

1. 指定された仕様を満たしている。
2. 不正状態が観測可能である。
3. 設計ルールに違反していない。
4. 対象コードをレビューしている。
5. 必要な検証を実行している。

ことを確認する。

バグが発生した場合は、最初に今回の変更の中に設計ルール違反がないか確認する。

ポリシー違反した実装がバグの原因である場合、互換性維持のために温存せず削除・再実装する。

---

## C++ 規約

- メンバ変数は `m_variableName`。
- クラス名は `PascalCase`。
- 関数名は `camelCase`。
- 定数は `UPPER_SNAKE_CASE`。
- テンプレート以外の大きな実装をヘッダへ書かない。
- 基本コンテナには `std::vector` を使用する。
- 移動可能な所有権の移譲には `std::move` を使用する。
- 意味のない一行化を行わない。
- 不正値を無言で捨てない。
- 既存の共通処理がある場合は再利用する。
- C++23 の機能を使用してコードを単純化できる場合は利用してよい。

---

## Character / Physics に関する注意

- 現行物理実装は Box3D を基準とする。
- Instance が存在することと native Box3D object が正常に存在することを同一視しない。
- body / joint / constraint の生成失敗を silent return しない。
- SceneLoader の Instance reference 解決失敗を無視しない。
- `Motor6D`、`BallSocket`、`Gyro` 等の constraint は、Part / body / anchor / native joint の状態を追跡可能にする。
- 親子階層と物理的拘束を混同しない。
- 親子関係だけを理由に BaseCube を追従させない。
- 物理的な追従が必要な場合は Weld / Motor6D / その他の constraint を使用する。
- キャラクターアニメーションのために body の CFrame を毎フレーム直接追従させる実装を追加しない。

---

## progress.md

長時間作業では、まとまったフェーズを完了した時点で `progress.md` に以下を記録する。

- 完了した変更
- 未完了の変更
- 現在壊れている箇所
- 実行した検証
- 判明している原因
- 次に行う具体的な一手

コンテキスト圧縮・利用上限・セッション終了後でも、過去の会話を再調査せず `progress.md` から作業再開できる状態を維持する。

再開時に既に `progress.md` に書かれている調査を最初からやり直さない。

---

## 検証

Release ビルドは WSL2 から以下を実行する。

```text
cmd.exe /d /c py build.py build
```

- 変更に対応する `RecubinTest` の回帰オプションが存在する場合、その対象テストだけを実行する。
- 変更に対応する回帰テストを限定できない場合、代わりに全回帰テストを実行しない。
- その場合は必要な手動検証内容を報告する。
- `cmd.exe /d /c py run_regression.py Release` による全回帰テストは、メインリリース時にユーザーが実行する。
- Codex は明示的に依頼された場合を除き全回帰テストを実行しない。
- Runtime の不具合を調査する場合、推測だけで修正せず、必要なら局所的な診断ログを追加して原因を観測する。
- 一時的な診断ログは原因特定後に削除するか、恒久的に価値があるログだけ適切なログレベルで残す。

---

## 実装後の報告

簡潔に以下を報告する。

- 変更した内容
- 原因
- 変更した主要ファイル
- 実行したビルド・テスト
- 未検証事項
- 残っている問題

「完了しました」と報告する前に、自分が変更したコードを再度読む。