# AGENTS.md — Recubin Codex 作業規約

## プロジェクト前提

- C++23 と OpenGL で実装する独自ゲームエンジン。
- 主環境は Windows 11。Codex は WSL2 から Windows 側のビルドを実行する。

## セッション開始時

1. `progress.md` の末尾にある最新項目と未解決事項を読む。
2. 変更対象に関係する `spec.md` と `doc/` 配下の文書を読む。
3. 実装前に、変更するファイルと箇所を列挙する。仕様が曖昧なら編集せず質問する。

## 作業範囲と役割

- 指示されたファイル・関数だけを変更し、便乗した最適化、整理、リファクタを行わない。
- 既存コメント、関数シグネチャ、ユーザーの未コミット変更を保護する。変更が不可避なら先に確認する。
- 計画・設計と影響範囲の判断はメインエージェントが行う。
- 設計確定後の実装・編集は `implementer` に具体的な対象と変更内容を渡して委譲し、メインエージェントが成果をレビューする。メインエージェント自身の編集は、成果物の微修正または 1〜2 行の軽微な変更に限る。
- `CLAUDE.md` と `.claude/` は Claude 用設定としてそのまま保持し、Codex の必須参照にせず、変更・削除しない。

## C++ 規約

- メンバ変数は `m_variableName`、クラス名は `PascalCase`、関数名は `camelCase`、定数は `UPPER_SNAKE_CASE` とする。
- テンプレート以外の実装をヘッダへ書かない。
- 例外を使わず、リターンコードまたはオプション型でエラーを表す。
- 基本コンテナには `std::vector` を使う。
- 所有権には原則 `std::unique_ptr` を使い、移動には `std::move` を使う。
- `std::shared_ptr` は PhysX オブジェクトなど共有所有が必須の場合だけ使う。
- 借用は `T&` を優先し、`nullptr` を許容する非所有参照または C API 連携だけに `T*` を使う。

## 検証

- Release ビルドは WSL2 から `cmd.exe /d /c py build.py build` を実行する。
- 変更に対応する `RecubinTest` の回帰オプションを実行する。対象を限定できない場合は `cmd.exe /d /c py run_regression.py Release` を実行する。
- 実装後の報告は、変更点と検証結果を簡潔に記載する。
